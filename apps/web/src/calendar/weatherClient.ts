/** Re-exports the unified weather client (kept for back-compat with calendar imports). */
export { fetchFarmCurrent, fetchFarmForecast } from "@/weather/weatherClient";
export type { WeatherCurrentPayload, WeatherForecastPayload } from "@/weather/weatherClient";
