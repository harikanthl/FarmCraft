/**
 * Google Calendar OAuth + sync is intentionally deferred (high complexity, low differentiation).
 * See project calendar plan — implement only after farm-native scheduling is proven.
 */
export const GOOGLE_CALENDAR_SYNC_DEFERRED = true;
