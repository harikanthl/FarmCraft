# Placeholder image — replace with Planetiler + Tippecanoe toolchain as needed.
FROM alpine:3.20
RUN echo "farmdots build-pmtiles: add Planetiler/Tippecanoe stages and publish to R2" > /NOTICE.txt
CMD ["cat", "/NOTICE.txt"]
